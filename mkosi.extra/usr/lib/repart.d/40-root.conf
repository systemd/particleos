# SPDX-License-Identifier: LGPL-2.1-or-later

[Partition]
Type=root
Format=btrfs
SizeMinBytes=1G
Subvolumes=/home /var
# Make sure /var/lib exists so /usr/etc/machine-id can be a symlink a file
# inside it.
MakeDirectories=/home /var/lib
Encrypt=tpm2
MakeSymlinks=/etc:../usr/etc
