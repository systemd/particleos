#!/bin/bash
# SPDX-License-Identifier: LGPL-2.1-or-later
set -e

cp --archive --no-target-directory "$BUILDROOT/etc" "$BUILDROOT/usr/share/factory/etc"
rm "$BUILDROOT/usr/share/factory/etc/machine-id"
ln -s /var/lib/machine-id "$BUILDROOT/usr/share/factory/etc/machine-id"
